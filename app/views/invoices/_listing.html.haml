%ul#invoices-table
  %li.header
    %span.number number
    %span.created created
    %span.due due
    %span.client client
    %span.title title
    %span.total total
  - @invoices.each do |invoice|
    %li
      = link_to invoice_path(invoice) do
        %span.number= invoice.number
        %span.created= format_date(invoice.created_at)
        %span.due= format_date(invoice.due_at)
        %span.client= invoice.client.name
        %span.title= invoice.title
        %span.total= format_currency(invoice.total)
  %li.footer
    %span.outstanding Outstanding
    %span.total= format_currency(Invoice.outstanding.collect(&:total).sum)
